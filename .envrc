#!/usr/bin/env bash
# direnv environment for riff-cli

# Load project hooks (Python venv, PYTHONPATH, etc.)
if [ -f .hookrc ]; then
  # shellcheck disable=SC1091
  source .hookrc
fi

echo "direnv: loaded .envrc (riff-cli)"
